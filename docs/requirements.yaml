name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - geopandas
  - pygeos
  - scipy
  - scikit-learn
  - networkx
  - pandana


    # Pip-only installs
  #- pip:

