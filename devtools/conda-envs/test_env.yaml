name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - geopandas
  - pygeos
  - scipy
  - sklearn
  - networkx
  - pandana

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

